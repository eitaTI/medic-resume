# Task 6: `.gitignore` para Backups e Uploads

✅ **Concluído** — `backups/` e `data/uploads/` adicionados ao `.gitignore`

Validado com `git check-ignore backups/x data/uploads/y` (ambos ignorados).

## Commit

```
chore: ignorar backups e uploads no .gitignore
```
