# Task 4: Testar Backup/Restore

❌ **Pendente** — validação manual do fluxo

Testar:
- Executar `./scripts/backup.sh` e verificar arquivos criados em `backups/`
- Executar `./scripts/restore.sh` sem argumentos e confirmar listagem
- Executar `./scripts/restore.sh <TIMESTAMP>` e verificar banco + uploads restaurados
- Verificar logs de execução

## Commit

```
test(backup): testar scripts de backup e restauração manualmente
```
