# Task 2: .dockerignore

❌ **Pendente** — criar `.dockerignore`

Criar `.dockerignore` excluindo: `node_modules`, `.next`, `.git`, `.env`, `.env.local`, `backups`, `data/uploads/*.db`, `*.db-journal`

## Commit

```
chore(docker): criar .dockerignore para build otimizado
```
