# Task 8: Better Auth Configurado

✅ **Concluído** — `lib/auth.ts`

Criar `lib/auth.ts`:
- Configurar `betterAuth` com `prismaAdapter` (SQLite)
- Habilitar `emailAndPassword`

## Commit

```
feat(auth): configurar Better Auth com Prisma adapter e email/password
```
