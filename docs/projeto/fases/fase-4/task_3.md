# Task 3: Componente SubmissaoCard

❌ **Pendente** — criar `components/admin/SubmissaoCard.tsx`

Criar `components/admin/SubmissaoCard.tsx`:
- Componente que recebe `submissao` com: `id`, `nomeClinica`, `nomeTitular`, `status`, `createdAt`, `medicos` (array com `id`)
- Card clicável que linka para `/admin/submissao/${id}`
- Exibir: nome da clínica (bold), StatusBadge, titular, quantidade de médicos, data formatada (pt-BR)
- Usar `StatusBadge` de `@/components/ui/StatusBadge`

## Commit

```
feat(admin): criar SubmissaoCard com link para detalhe
```
